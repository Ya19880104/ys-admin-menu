# Changelog

本外掛所有重要變更皆記錄於此。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)，版本號遵循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [1.2.3] - 2026-07-28

### Fixed

- Stop the bundled YS Hub Client library from registering an invalid
  WooCommerce HPOS declaration from its vendor path.

## [1.2.2] - 2026-07-22

### 修正
- **部分外掛選單被強制展開、收不起來**：某些外掛（例如 WPForms 的 Setup Checklist）會在每個後台頁面注入高優先度樣式，強制撐開自己的子選單，蓋過本外掛的收放控制。現在收合狀態一律由選單收放控制決定，這類選單可正常收起；展開時仍保留該外掛自訂的選單版面。側欄收合（icon）模式下的原生飛出子選單不受影響。

## [1.2.1] - 2026-07-22

### 新增
- **拖拉提升子選單為主選單**：「全部選單（含子選單）」分頁頂部新增「提升區」——用最左側 ⋮⋮ 把子選單列拖進該區，即提升為 wp-admin 頂層選單；拖回下方即取消。與原有的「升頂層」勾選框雙向同步（兩種操作皆可、狀態一致）。

## [1.2.0] - 2026-07-22

### 新增
- **匯入／匯出設定**：「選單與權限」頁工具列新增「匯出設定」「匯入設定」——可把目前全部設定（選單＋權限、原生選單樣式、白牌）打包成 JSON 檔備份，或從備份檔還原（匯入前會確認並建議先備份）。方便跨站複製或版本回復。
- **拖拉排序提示**：工具列新增說明，指出可拖拉每列最左側的 ⋮⋮ 圖示調整順序。

### 修正
- **「全部選單」自訂顯示名稱儲存後被清空**：修正在「全部選單（含子選單）」分頁替選單／子選單填「自訂顯示名稱」後，一儲存就被清掉的問題（先前後端 sanitize 遺漏此欄位）；現在會正確保存並套用到側欄（含子選單改名）。

## [1.1.2] - 2026-07-22

### 修正
- **提升項在「全部選單」的顯示**：已提升的子頁在「全部選單（含子選單）」一律顯示為原父層底下的子頁列（勾選狀態可取消還原），不再以提升後的頂層身分重複出現；修正 .php 型子頁提升後設定列消失、無法取消提升的問題。

## [1.1.1] - 2026-07-22

### 修正
- **提升頁側欄高亮唯一化**：造訪 hook 型提升頁（原生網址為「父層.php?page=slug」）時，原父層選單不再同時帶目前狀態與展開樣式；高亮唯一落在提升後的頂層項。

## [1.1.0] - 2026-07-22

### 新增
- **子選單提升為主選單（升頂層）**：「全部選單（含子選單）」分頁的每個子選單項新增「升頂層」勾選——勾選後該子頁改顯示為 wp-admin 頂層選單（原位置移除、icon 沿用原父層）。連結沿用原生網址組合，頁面載入、權限與功能完全不受影響；提升後可在「主選單（頂層）」分頁如一般頂層項排序、套色、限制角色（並標示「已提升」）。造訪該頁時側欄高亮會正確落在提升後的頂層項。取消勾選即還原為原本的子選單。

## [1.0.14] - 2026-07-16

### Fixed
- Become the exclusive menu-router and permission-settings owner when YS CART is also active, while continuing to let YS CART own the admin-theme runtime.
- Import the legacy core menu config only when the standalone option does not exist, mirror every authoritative save back to the core fallback option, and perform a final mirror on deactivation.
- Prevent an intentionally empty standalone config from being replaced by stale core data.
- Preserve the existing "YS CART only" user restriction by delegating core route classification when YS CART is active and falling back to configured YS CART menu items otherwise.

## [1.0.13] - 2026-07-16

### Fixed
- When YS CART and YS Admin Menu are both active, YS CART exclusively owns the admin theme runtime. Menu routing, permissions, settings, and white-label modules remain active without duplicate theme CSS/JS injection.

## [1.0.12] - 2026-07-06

### 修正
- **子選單排序：未排序項目一致沉到底部**：接續 1.0.10／1.0.11，子選單重排改為可預測規則——父選單自身項（如「設定 → 一般」）永遠置頂、有設定順序的子項照設定排列、尚未列入設定的子項一律排到子選單最後（保留相對順序）。避免未列入設定的新子頁夾在已排序項目之間。

## [1.0.11] - 2026-07-06

### 修正
- **子選單排序：未排序項目保留原位**：接續 1.0.10，改善子選單重排——未在設定中排序的子項（例如「設定 → 一般」這種與父選單同名的項目，或稍後才安裝的外掛子頁）會保留在原本的位置，不再被推到子選單最後面；有設定順序的子項則在其原本佔用的位置間依序重排。

## [1.0.10] - 2026-07-06

### 修正
- **子選單排序未生效**：修正在「全部選單（含子選單）」設定拖拉／設定子選單（sub-page）順序後，實際 wp-admin 側欄子選單順序未跟著改變的問題。先前路由器只重排頂層選單，未套用子選單順序；現在會依設定的順序重排各頂層底下的子選單。

## [1.0.9] - 2026-07-06

### 改進
- **分頁頁籤明確化**：「選單與權限」頁上方三個分頁（主選單／全部選單／使用者覆寫）改為分段膠囊按鈕樣式，作用中頁籤以 indigo 實心標示，不再像純文字連結。
- **設定頁首標題升級**：三頁共用的頁首橫幅改為三段漸層＋柔光層次＋玻璃質感圖示，更精緻一致。
- **主／子選單層級對比**：「全部選單（含子選單）」清單中，子選單項改為縮排＋樹狀連接線＋淡化字重，與頂層選單（粗黑）明顯區隔，層級與排序關係一眼可辨。

## [1.0.8] - 2026-07-06

### 新增
- **子選單面板左縮排可控**：展開子選單整體相對主選單的左縮排（0–80px，預設 42，原為固定值），於「子選單項」卡片調整；可縮小以加寬子選單可用寬度。

## [1.0.7] - 2026-07-06

### 新增
- **子選項文字粗細**：子選單項可調 font-weight（100–900，預設 500，原為固定值）。

### 改進
- **子選單文字控制集中**：「子選單字級」「子選單行高」自「文字大小」區移入「子選單項（展開後的選項）」卡片，子選項的字級／粗細／行高／留白／hover 集中一處調整；「文字大小」區更名為「文字大小（主選單）」。

## [1.0.6] - 2026-07-06

### 修正
- **首次點擊選單無反應**：修正在觸控裝置（含觸控螢幕筆電／桌機）上，點擊有子選單的頂層選單名稱時，第一次點擊被 WordPress 原生觸控行為攔截（畫面跳一下但不導航），需點第二次才進入頁面的問題；現在第一次點擊即直接導航，右側箭頭區維持收放子選單。
- **子選單跳動防護**：展開中的子選單不再因原生腳本注入位移或 hover 展開狀態而收合跳動。

## [1.0.5] - 2026-07-06

### 變更
- **刪除外掛預設保留設定**：解除安裝外掛時預設不再清除設定，重新安裝即沿用原本的選單與樣式設定；新增「刪除外掛時一併清除所有設定」開關（白牌設定頁），需明確勾選才會於刪除外掛時清除。

## [1.0.4] - 2026-07-06

### 新增
- **分組標籤外距（margin）**：分組標籤可設定上／右／下／左外距，調整與相鄰選單項的間隔。

## [1.0.3] - 2026-06-18

### 新增
- **分組標籤樣式控制**：原生選單樣式頁新增「分組標籤」外觀設定——可自訂側欄分組標題（帶標題分隔列）的文字色、背景色、字級、粗細，以及上／右／下／左各自獨立的留白。
- **選單項留白控制**：新增一般選單項的上／右／下／左內距設定。
- **子選單項控制**：展開後的子選單項可調上／右／下／左留白與 hover 背景色（子選單整體背景色沿用「子選單背景色」）。
- **展開／運作中選單背景色**：手動展開的選單與目前所在頁（運作中）的選單，可各自設定不同背景色，清楚區隔展開與運作狀態。

### 修正
- 分組標籤不再有 hover 反白效果，維持靜態外觀（含深色皮膚啟用時）。

## [1.0.2] - 2026-06-18

### 改進
- **設定介面美化（三頁統一）**：選單與權限、原生選單樣式、白牌設定三頁套用統一的 indigo 品牌頁首列；白牌設定頁啟用完整 design system（卡片、按鈕、區塊標題色條），原生 checkbox 改為質感 toggle 開關。

## [1.0.1] - 2026-06-18

### 新增
- **白牌 — 隱藏 WordPress LOGO**：可隱藏 admin bar 左上角的 WordPress 標誌（含關於／官網／文件等子選單）。
- **白牌 — 整個後台背景色**：可自訂 wp-admin 內容區背景色，設定後立即套用（不需啟用主題皮膚）。
- **權限 — 僅限本人**：選單可設為「僅限當前帳號可見」，綁定後僅該帳號可見（側欄與直接網址皆生效，且優先於 super-admin）。

### 修正
- **分隔列**：修正設定頁分隔列未依排序顯示、新增時不即時顯示的問題；有標題的分隔列現在會在側欄顯示為分組標籤。
- **選單列舉重複**：修正帶標題的分隔列被當成真實選單、在設定頁重複列出的問題。

## [1.0.0] - 2026-06-17

### 新增
- **選單編輯**：wp-admin 左側選單拖拉排序、隱藏／顯示、重新命名、改色、插入分隔列（頂層與子選單）。
- **權限控管**：依角色顯示／隱藏選單、個別使用者白名單覆寫、直接網址存取 404 防護。
- **原生選單樣式**：側欄與 admin bar 配色、字級、行高自訂，含即時預覽與一鍵重設。
- **白牌**：上傳 LOGO 作為頂層選單圖示、自訂 wp-admin 頁尾文字。
- 整合 YS Plugin Hub Client 支援自動更新。

### 說明
- 本版自 YS CART 抽離既有後台選單管理子系統，重構為可獨立運作的外掛；視覺與互動沿用 YS CART 既有設計（ADR-049／050 design system）。
- 設定改用獨立的 `ys_admin_menu_*` option 儲存，與 YS CART 完全隔離。
